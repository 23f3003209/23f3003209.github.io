name: Cache Test

on: [push]

jobs:
  cache-job:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Cache dependencies
        id: cache-step
        uses: actions/cache@v4
        with:
          path: ~/.cache
          key: cache-f954c21

      - name: prime-cache-f954c21
        run: echo "Cache hit? -> ${{ steps.cache-step.outputs.cache-hit }}"
